apiVersion: v1
kind: Service
metadata:
  name: production-service
  namespace: business-management
  labels:
    app: production-service
spec:
  type: ClusterIP
  ports:
  - port: 8005
    targetPort: 8005
    protocol: TCP
    name: http
  selector:
    app: production-service
