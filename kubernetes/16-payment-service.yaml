apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: business-management
  labels:
    app: payment-service
spec:
  type: ClusterIP
  ports:
  - port: 8003
    targetPort: 8003
    protocol: TCP
    name: http
  selector:
    app: payment-service
