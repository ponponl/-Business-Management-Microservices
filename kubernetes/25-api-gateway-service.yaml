apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: business-management
  labels:
    app: api-gateway
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
    protocol: TCP
    name: http
  selector:
    app: api-gateway
