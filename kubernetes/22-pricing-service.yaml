apiVersion: v1
kind: Service
metadata:
  name: pricing-service
  namespace: business-management
  labels:
    app: pricing-service
spec:
  type: ClusterIP
  ports:
  - port: 8006
    targetPort: 8006
    protocol: TCP
    name: http
  selector:
    app: pricing-service
